/**
 * Shared application constants.
 * - Validation limits
 * - API defaults and cross-feature window sizes
 */
export const MIN_PASSWORD_LEN = 8

// API defaults
export const DEFAULT_PER_PAGE = 100

// Calendar/Reminders windows (used across sidebar and calendar if adopted)
export const UPCOMING_WINDOW_DAYS = 30
export const EVENTS_FETCH_PAD_DAYS = 45